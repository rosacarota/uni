#include <stdio.h>
#define N 10
int main(){
       int i, j, f, a[N], b[N];

       for(i = 0; i < N; i++){
           scanf("%d", &a[i]);
        }
                                                                        
       f=0;
       for (i = 0; i < n; i++){
           for(j=0; j <= f; j++){
               if (a[i] == b[j]){
                   break;
                else{
                    printf("%d", b[j]);
                }
